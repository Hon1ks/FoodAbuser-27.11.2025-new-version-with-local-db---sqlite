
# Инструкция по работе с Git и GitHub для проекта Dungeon Master Web

## 📦 1. Первый запуск (инициализация и загрузка проекта на GitHub)

> Делается **один раз** при первом заливании проекта

```bash
# Перейди в папку с проектом
cd D:/Projects/Food Abuser

# Инициализируй git
git init

# Добавь ссылку на свой репозиторий на GitHub
git remote add origin https://github.com/ТВОЙ_ЮЗЕРНЕЙМ/ИМЯ_РЕПО.git

# Добавь все файлы
git add .

# Сделай первый коммит
git commit -m "Первый коммит: добавлен рабочий прототип Приложения"

# Назначь основную ветку main
git branch -M main

# Залей на GitHub
git push -u origin main
```

---

## 🔄 2. Как обновить проект на GitHub после изменений

> Делается **всякий раз**, когда ты что-то поменял или добавил

```bash
# Проверить, какие файлы изменились
git status

# Добавить все изменения
git add .

# Сделать коммит с описанием
git commit -m "Добавлено: GameRoom, логика выбора персонажа, анимации"

# Отправить изменения на GitHub
git push
```

---

## 💡 Полезные команды

| Команда                        | Что делает                             |
|-------------------------------|----------------------------------------|
| `git status`                  | Показывает изменённые файлы            |
| `git add .`                   | Добавляет все изменения в коммит       |
| `git commit -m "..."`         | Создаёт точку сохранения               |
| `git push`                    | Отправляет изменения на GitHub         |
| `git pull`                    | Забирает изменения из GitHub           |
| `git log`                     | История всех коммитов                  |

---

## ✅ Рекомендации

- Перед коммитом всегда запускай `git status`
- Пиши **понятные комментарии** к коммитам
- Не заливай `node_modules`, `.env`, `dist` — они должны быть в `.gitignore`
- Если забыл добавить файл — можно снова сделать `add` и `commit`
